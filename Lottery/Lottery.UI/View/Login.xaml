<Window x:Class="Lottery.UI.View.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lottery.UI.View"
        mc:Ignorable="d"
        xmlns:language="clr-namespace:Loterry.UI.Properties.Langs"
        Title="Login" Height="600" Width="500">
    <Window.Resources>
        <Style x:Key="InputFieldStyle" TargetType="{x:Type Control}">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="35,8,8,8"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Control}">
                        <Border CornerRadius="8" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox" BasedOn="{StaticResource InputFieldStyle}"/>
        <Style TargetType="PasswordBox" BasedOn="{StaticResource InputFieldStyle}"/>

        <Style x:Key="IconStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="IsHitTestVisible" Value="False"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Border BorderBrush="#DDDDDD" BorderThickness="1" Background="White" CornerRadius="12" Padding="40" MaxWidth="420" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="1" Color="Black" Opacity="0.1" BlurRadius="10"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="{x:Static language:Lang.LoginLabelTitle}" Style="{StaticResource TitleTextStyle}"/>
                <TextBlock Text="{x:Static language:Lang.LoginLabelEnterDetail}" Style="{StaticResource SubtitleTextStyle}"/>

                <Label Content="{x:Static language:Lang.GlobalLabelEmail}" Style="{StaticResource LabelStyle}" Target="{Binding ElementName=EmailTextBox}"/>
                <Grid>
                    <TextBox x:Name="EmailTextBox" Text="carlos@gmail.com"/>
                    <TextBlock Text="&#xE715;" Style="{StaticResource IconStyle}"/>
                </Grid>

                <Label Content="{x:Static language:Lang.GlobalLabelPassword}" Style="{StaticResource LabelStyle}" Target="{Binding ElementName=PasswordBox}"/>
                <Grid>
                    <PasswordBox x:Name="PasswordBox" Password="password123"/>
                    <TextBlock Text="&#xE72E;" Style="{StaticResource IconStyle}"/>
                </Grid>

                <Label Content="{x:Static language:Lang.GlobalLabelConfirmPassword}" Style="{StaticResource LabelStyle}" Target="{Binding ElementName=ConfirmLoginPasswordBox}"/>
                <Grid>
                    <PasswordBox x:Name="ConfirmLoginPasswordBox" Password="password"/>
                    <TextBlock Text="&#xE72E;" Style="{StaticResource IconStyle}"/>
                </Grid>

                <Button x:Name="StartButton" Click="StartButton_Click" Content="{x:Static language:Lang.GlobalButtonStart}" Margin="0,30,0,0" Padding="12" FontSize="16" FontWeight="Bold" Foreground="White" Background="Black" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" Margin="0,20,0,0" HorizontalAlignment="Center" Cursor="Hand">
                    <TextBlock Text="{x:Static language:Lang.LoginButtonForgotMyPassword}" Foreground="Gray" FontSize="13"/>
                </Button>

                <Button x:Name="SignUpButton" Click="SignUpButton_Click" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" HorizontalAlignment="Center" Cursor="Hand">
                    <TextBlock Text="{x:Static language:Lang.LoginButtonSignUp}" Foreground="Gray" FontWeight="SemiBold" FontSize="14"/>
                </Button>
            </StackPanel>
        </Border>

    </Grid>
</Window>