<Window x:Class="Lottery.UI.View.JoinLobbyView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lottery.UI.View"
        mc:Ignorable="d"
        Title="Unirse a una sala" Height="450" Width="500"
        Background="#F5F5F5"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Border BorderBrush="#DDDDDD" 
                BorderThickness="1" 
                Background="White" 
                CornerRadius="12" 
                Padding="40" 
                MaxWidth="420"
                VerticalAlignment="Center" 
                HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="1" Color="Black" Opacity="0.1" BlurRadius="10"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="Unirse a una sala" Style="{StaticResource TitleTextStyle}"/>
                <TextBlock Text="Ingresa el código de la sala" Style="{StaticResource SubtitleTextStyle}" Margin="0,5,0,20"/>

                <Label Content="Código de sala" 
                       Style="{StaticResource LabelStyle}" 
                       Target="{Binding ElementName=LobbyCodeTextBox}"/>

                <TextBox x:Name="LobbyCodeTextBox" 
                         Text="0000" 
                         TextAlignment="Center"
                         FontSize="16"
                         FontWeight="SemiBold"/>

                <Button x:Name="JoinLobbyButton" 
                        Click="JoinLobbyButton_Click" 
                        Content="Unirme a la sala" 
                        Margin="0,30,0,0" 
                        Padding="12" 
                        FontSize="16" 
                        FontWeight="Bold" 
                        Foreground="White" 
                        Background="Black" 
                        BorderThickness="0" 
                        Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>